# 🤖 Cursor AI - Правила разработки

## 🎯 ПРИ РАБОТЕ С КОДОМ

### ✅ ВСЕГДА ДЕЛАТЬ:
1. **Разбивать большие компоненты** на мелкие модули (макс. 100 строк на файл)
2. **Использовать Tailwind CSS** для всех стилей (никогда inline styles)
3. **Применять адаптивность**: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
4. **Контейнеры**: `max-w-[1280px] mx-auto px-6`
5. **Создавать feature-based структуру** в `src/modules/`
6. **Выносить логику** в custom hooks в `src/modules/{feature}/hooks/`
7. **Тестировать на всех разрешениях**: mobile (375px), tablet (768px), desktop (1280px)

### ❌ НИКОГДА НЕ ДЕЛАТЬ:
1. Создавать монолитные страницы с 200+ строками кода
2. Использовать inline styles вместо Tailwind
3. Фиксированные размеры без адаптивности
4. Дублировать код - выносить в компоненты/hooks
5. Оставлять `TODO` комментарии без реализации
6. Пропускать обработку ошибок (try/catch, error states)

### 📁 СТРУКТУРА КОМПОНЕНТОВ
```
Правильно:
src/modules/home/components/WebinarModule.jsx (модульный компонент)

Неправильно:
src/pages/Home.jsx (160 строк всей логики)
```

### 🎨 ADAPTIVE PATTERNS
```jsx
// Breakpoints
sm: 640px    // мобильные горизонтально
md: 768px    // планшеты
lg: 1024px   // ноутбуки
xl: 1280px   // десктопы

// Grid пример
grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4

// Текст
text-sm md:text-base lg:text-lg

// Скрытие
hidden md:block    // desktop only
block md:hidden    // mobile only
```

### 🧩 КОМПОНЕНТНАЯ АРХИТЕКТУРА
```jsx
// ✅ ХОРОШО: Модульно
export default function Home() {
  return (
    <div className='grid grid-cols-1 lg:grid-cols-3'>
      <WebinarModule />
      <CoursesModule />
      <NotificationsModule />
    </div>
  )
}

// ❌ ПЛОХО: Монолит
export default function Home() {
  // ... 200 строк кода
}
```

### 🔐 БЕЗОПАСНОСТЬ
- Все API запросы через `apiClient` (авт. добавление токена)
- Protected routes через `<ProtectedRoute>`
- Валидация данных на backend (DTO)

### 🚀 BACKEND
- Использовать DTO для валидации
- Сервисы для бизнес-логики
- Guards для защиты роутов
- Prisma для работы с БД

### 📦 ПЕРЕИСПОЛЬЗОВАНИЕ
- UI элементы → `src/components/`
- Feature логика → `src/modules/{feature}/`
- Переиспользуемые хуки → `src/modules/{feature}/hooks/`
- API методы → `src/modules/{feature}/services/`

### 🎨 ЦВЕТА
```jsx
// Primary (Cyan)
bg-cyan-500 text-cyan-700 border-cyan-300

// Success
bg-green-500 text-green-700

// Error  
bg-red-500 text-red-700

// Gray
bg-gray-100 text-gray-800
```

### 📝 COMMIT FORMAT
```
feat: добавить модуль авторизации
fix: исправить адаптивность на мобильных
refactor: разбить Home.jsx на компоненты
```

### 🔄 ПРОЦЕСС РАЗРАБОТКИ
1. Создать задачу/функционал
2. Разбить на модули/компоненты
3. Создать структуру в `src/modules/`
4. Реализовать адаптивность
5. Протестировать на всех размерах
6. Проверить линтер
7. Commit

---

📚 Подробнее: см. CURSOR_RULES.md и ARCHITECTURE_GUIDE.md

